# HTTP Example Payloads

Captured JSON examples for the `/v0` HTTP contract.

These examples are compatibility anchors for docs and future conformance checks.
They are additive in this wave and will be expanded alongside endpoint validation.
